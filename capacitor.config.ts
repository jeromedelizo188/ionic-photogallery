import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicframework.photogallery',
  appName: 'photo-gallery ',
  webDir: 'dist'
};

export default config;
